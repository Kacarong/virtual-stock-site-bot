[project]
name = "papertrade-backend"
version = "0.1.0"
description = "Paper trading backend + worker + bot"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]
